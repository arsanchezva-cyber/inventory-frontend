{
    "version": 2,
    "buildCommand": "npm run build",
    "outputDirectory": "dist/frontend",
    "installCommand": "npm install --legacy-peer-deps",
    "framework": "angular",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}